sudo: required
services:
  - docker

before_install:
  - docker build -t mustafansrv/docker-react-frontend -f Dockerfile.dev .

script:
  - docker run -e CI=true mustafansrv/docker-react-frontend npm run test